{
  "name": "be",
  "private": true,
  "scripts": {
    "dev": "pm2 start pm2.config.cjs && pm2 save && pm2 monit"
  },
  "dependencies": {
    "hp-shared": "workspace:^",
    "shared": "workspace:^",
    "mongodb": "^6.6.1"
  }
}
