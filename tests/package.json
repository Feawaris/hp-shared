{
  "name": "tests",
  "private": true,
  "scripts": {
    "test": "pnpm -C ../examples/shared run build && jest"
  },
  "dependencies": {
    "hp-shared": "workspace:^",
    "shared": "workspace:^",
    "jest": "^29.7.0"
  }
}
