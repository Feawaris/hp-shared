{
  "name": "tests",
  "private": true,
  "scripts": {
    "test": "jest",
    "helpers/be": "pnpm -C helpers/be run dev",
    "helpers/fe": "npm-run-all -p 'helpers/fe-*'",
    "helpers/fe-browser": "pnpm -C helpers/fe-browser run dev",
    "helpers/fe-wx": "pnpm -C helpers/fe-wx run dev"
  },
  "dependencies": {
    "hp-shared": "workspace:*",
    "jest": "^29.7.0"
  }
}
